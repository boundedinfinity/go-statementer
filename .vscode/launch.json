{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "docsorter",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "args": ["${workspaceFolder}/config.yaml"]
        }
    ]
}